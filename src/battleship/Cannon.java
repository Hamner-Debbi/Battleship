/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package battleship;

/**
 *
 * @author Debbi
 */
public class Cannon {
     // Ask the user to enter the x and y coordinate.
      public String x = get("x");//(0-9) should this be int or byte x?
      public String y = get("y");//(A-J) should this be char y?
        //coordinate will interface with the ship which will
        //interface with the board
        if (x, y) = hit;
            System.out.println("hit");
        else (x, y)= miss;
             System.out.println("miss");         
              
            
}
    public class Life {
        public startingLife = 25;
        public hit = -1;
        public miss = 0;//no calculations required after a miss
            if (hit = 25; hit > 0; hit --1); 
                System.out.println("You are still alive." );
            else (hit = 25); hit =<0; hit==0);
                System.out.println("You are dead.");
    }
    public class Remaining Life  {  
            //after first turn
            startingLife - hit = remainingLife;
            //calculates life for consecutive turns after first
            remainingLife - hit = newRemainingLife;
            //Displays the % of life remaining after the computers turn
            remainingLife / startingLife = percentRemainingLife;
                System.out.println("% of life left is" + percentRemainingLife);
                     
    }
    

   




//elements of trajectory
    //public String speed;
    //public String height;
    //public String direction;
    //public String mass;
    
  // Initial speed at which the bullet moves vertically
    

    //private static class speed {
        //
        //double mass = 26;//lb
            
  
   
